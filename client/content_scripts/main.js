// has access to DOM APIs for its host page (the page it runs - specified as matches in manifest.json)...

/* TODOS (V1):
    Components:
    - Create Search Button
    - Create Search Input Field
    - Create List Component for Search Results

    Actions:
    - Extract uploaded caption
    - Implement local search
    - Dump video meta to api endpoint
    - fetch meta about store indexes for video

    Events:
      When user clicks on search icon, 
        - load search bar component

      When user clicks result, 
        - seek video to timestamp

      When user types into the search bar, save search term in memory and when they press enter
        - fetch result set
*/

(function () {
  // code goes in here
})();
